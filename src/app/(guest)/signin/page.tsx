import { SignInForm } from '@components/auth/forms/signin-form';
import { Main } from '@components/layout';

const Page: React.FC = () => (
  <Main>
    <SignInForm />
  </Main>
);

export default Page;
