import { SignUpForm } from '@components/auth/forms/signup-form';
import { Main } from '@components/layout';

const Page: React.FC = () => (
  <Main>
    <SignUpForm />
  </Main>
);

export default Page;
