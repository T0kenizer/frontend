import { SignUpForm } from '@components/auth/forms/signup-form';
import { Main } from '@components/layout/main';

const Page: React.FC = () => (
  <Main>
    <SignUpForm />
  </Main>
);

export default Page;
