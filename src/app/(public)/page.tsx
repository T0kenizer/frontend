import { Main } from '@components/layout';
import { ThemeSwitcher } from '@components/theme/theme-switcher';

const Page: React.FC = () => (
  <Main>
    <ThemeSwitcher />
  </Main>
);

export default Page;
